<h1 class="title">Click a User to View Their Collection:</h1>
 
 <ul>
    <%@users.each do |user| %> 
        <div class="subtitle is-3">
        <strong> <li><%= link_to user.name, user_path(user.id)%></li> </strong>
        </div>
    <%end %>
 </ul>